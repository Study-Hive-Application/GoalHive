<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Todo List Application</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"
    />
    <link rel="stylesheet" href="styles/todo.css" />
  </head>

  <body class="bg-gray-100">
    <div>
      <!-- Header -->
      <%- include("partials/header") %>
    </div>

    <!-- Main Container -->
    <div class="container mx-auto p-4 pt-28">
      <!-- Three Sections Container -->
      <div class="grid grid-cols-2 gap-6">
        <!-- Tasks Section -->
        <div
          class="transparent-card shadow-md rounded-lg p-4"
          data-aos="zoom-in"
        >
          <h2 class="text-xl font-semibold mb-4 border-b pb-2">Tasks</h2>
          <div id="taskList" class="space-y-2">
            <!-- Tasks will be dynamically added here -->
          </div>
        </div>

        <!-- Create Task Section -->
        <div
          class="transparent-card shadow-md rounded-lg p-4"
          data-aos="zoom-in"
          data-aos-duration="1000"
        >
          <h2 class="text-xl font-semibold mb-4 border-b pb-2">Create Task</h2>
          <div class="space-y-4">
            <input
              type="text"
              id="taskInput"
              placeholder="Enter task description"
              class="w-full p-2 border rounded focus:outline-blue-500 bg-white/70"
            />
            <input
              type="date"
              id="taskDate"
              class="w-full p-2 border rounded focus:outline-blue-500 bg-white/70"
            />
            <button
              id="addTaskBtn"
              class="w-full bg-blue-500 text-white p-2 rounded hover:bg-blue-600 transition"
            >
              Add Task
            </button>
          </div>
        </div>
      </div>

      <!-- Completed Tasks Section (Full Width) -->
      <div
        class="transparent-card shadow-md rounded-lg p-4 mt-6"
        data-aos="zoom-in"
        data-aos-duration="2000"
      >
        <h2 class="text-xl font-semibold mb-4 border-b pb-2">
          Completed Tasks
        </h2>
        <div id="completedTaskList" class="space-y-2">
          <!-- Completed tasks will be dynamically added here -->
        </div>
        <!-- Show More Button -->
        <button
          id="showMoreBtn"
          class="hidden mt-4 bg-gray-200 text-gray-700 px-4 py-2 rounded hover:bg-gray-300 transition"
        >
          Show More
        </button>
      </div>
    </div>
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <script src="scripts/todo.js"></script>
  </body>
</html>
